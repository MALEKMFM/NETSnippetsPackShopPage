<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="description" content="Boost your .NET development with a curated pack of reusable C# code snippets for APIs, authentication, data, error handling, and more. Designed for speed, quality, and ease of use.">
    <meta name="keywords" content=".NET, C#, code snippets, productivity, API, authentication, entity framework, linq, developer tools, Visual Studio, VS Code">
    <meta name="author" content="Malek Mshinesh">
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-488502114"></script>
    <script>
        window.dataLayer = window.dataLayer || [];
        function gtag(){dataLayer.push(arguments);}
        gtag('js', new Date());
        gtag('config', 'G-488502114');
    </script>
    <link rel="canonical" href="https://techmsh.gumroad.com/l/netsnippetpack?_gl=1*12anfxf*_ga*MTg1OTA1NTQyNy4xNzQ2MjIyMDAw*_ga_6LJN6D94N6*czE3NDY2NTI4MzMkbzgkZzEkdDE3NDY2NTI4MzQkajAkbDAkaDA." />
    <meta property="og:title" content=".NET Productivity Snippet Pack — Supercharge Your .NET Coding">
    <meta property="og:description" content="Curated, reusable C# snippets for .NET developers. Boost productivity, reduce errors, and ship faster.">
    <meta property="og:image" content="https://public-files.gumroad.com/oospwifzo2mrrztu43cryazwe2ab">
    <meta property="og:url" content="https://techmsh.gumroad.com/l/netsnippetpack?_gl=1*12anfxf*_ga*MTg1OTA1NTQyNy4xNzQ2MjIyMDAw*_ga_6LJN6D94N6*czE3NDY2NTI4MzMkbzgkZzEkdDE3NDY2NTI4MzQkajAkbDAkaDA.">
    <link rel="icon" type="image/png" href="/assets/favicon-96x96.png" sizes="96x96" />
    <link rel="icon" type="image/svg+xml" href="/assets/favicon.svg" />
    <link rel="shortcut icon" href="/assets/favicon.ico" />
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />
    <meta name="apple-mobile-web-app-title" content="SnippetForge" />
    <link rel="manifest" href="/site.webmanifest" />
    <meta property="og:type" content="website">
    <script type="application/ld+json">
        {
          "context": "https://schema.org/",
          "type": "Product",
          "name": ".NET Productivity Snippet Pack",
          "image": [
            "https://yourdomain.com/path-to-your-main-product-image.png"
          ],
          "description": "Curated pack of reusable, high-quality C# code snippets for .NET developers. Accelerate API, auth, data, and more.",
          "brand": {
            "type": "Brand",
            "name": "YourBrand"
          },
          "offers": {
            "type": "Offer",
            "url": "https://yourdomain.com/",
            "priceCurrency": "USD",
            "price": "19",
            "availability": "https://schema.org/InStock"
          }
        }
    </script>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - NETSnippetsPackShopPage</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css" crossorigin="anonymous">
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="path/to/font-awesome/css/font-awesome.min.css">
    <link href="https://fonts.googleapis.com/css?family=Noto+Sans:400,700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="~/NETSnippetsPackShopPage.styles.css" asp-append-version="true" />
    <style>
        body {
            font-family: 'Not', Arial, Helvrtica, sans-serif;
        }

        h1, h2, h3 {
            font-family: 'Noto Sans', Arial, Helvetica, sans-serif;
            font-weight: 700;
        }
    </style>
</head>

<body>
    <!-- Sticky Buy Now Button at Top, Hidden by Default -->
    <a href="https://your-gumroad-link.com/" target="_blank"
       class="sticky-buy-btn animate-in" id="sticky-buy-btn">
        <i class="fas fa-shopping-cart" aria-label="Buy Now"></i>
        I Want This! – $10
    </a>
    <header>
        <nav class="navbar navbar-expand-sm navbar-toggleable-sm navbar-light bg-white border-bottom box-shadow mb-3">
            <div class="container-fluid">
                <a class="navbar-brand" asp-area="" asp-controller="Home" asp-action="Index">NETSnippetsPackShopPage</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target=".navbar-collapse" aria-controls="navbarSupportedContent"
                        aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="navbar-collapse collapse d-sm-inline-flex justify-content-between">
                    <ul class="navbar-nav flex-grow-1">
                        <li class="nav-item">
                            <a class="nav-link text-dark" asp-area="" asp-controller="Home" asp-action="Index">Home</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link text-dark" asp-area="" asp-controller="Home" asp-action="Privacy">Privacy</a>
                        </li>
                    </ul>
                </div>
            </div>
        </nav>
    </header>
    <div class="container">
        <main role="main" class="pb-3">
            @RenderBody()
        </main>
    </div>
    <button class="darkmode-switch" id="darkmode-switch" title="Toggle dark/light mode">🌓</button>

    <footer class="border-top footer text-muted">
        <div class="container">
            &copy; 2025 - NETSnippetsPackShopPage - <a asp-area="" asp-controller="Home" asp-action="Privacy">Privacy</a>
        </div>
    </footer>
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    <script>
                document.addEventListener("DOMContentLoaded", function () {
            var btn = document.getElementById('sticky-buy-btn');

            function toggleStickyBtn() {
                if (window.scrollY > 455) {
                    console.log("Button shows up");
                    btn.classList.add('animate-in');
                } else {
                    btn.classList.remove('animate-in');
                }
            }

            window.addEventListener('scroll', toggleStickyBtn);

            // Optional: make sure state is correct if page loads scrolled down
            toggleStickyBtn();
        });
        // document.addEventListener("DOMContentLoaded", function() {
        //   var btn = document.getElementById('sticky-buy-btn');
        //   // var isVisible = false;
        //   window.addEventListener('scroll', function() {
        //     if (window.scrollY > 455) {
        //         if(!btn.classList.contains('animate-in')){
        //             btn.classList.add('animate-in');
        //             // isVisible = true;
        //             console.log("ss");
        //         }
        //     } else {
        //          if(btn.classList.contains('animate-in')){
        //              btn.classList.remove('animate-in');
        //              // isVisible = false;
        //          }         
        //     }
        //   });
        // });
    </script>
    <script>
        document.addEventListener("DOMContentLoaded", function() {
            const reveals = document.querySelectorAll('.reveal');
            function revealOnScroll() {
                var winBottom = window.innerHeight + window.scrollY;
                reveals.forEach(function(el) {
                    if (el.getBoundingClientRect().top + window.scrollY < winBottom - 100) {
                        el.classList.add('visible');
                    }
                });
            }
            window.addEventListener('scroll', revealOnScroll);
            revealOnScroll();
        });
    </script>

    <script>
        document.addEventListener("DOMContentLoaded", function() {
            const lines = [
                "Supercharge your C# coding 🚀",
                "Import. Code. Win.",
                "Cleaner .NET projects in seconds.",
                "Crafted for real-world devs.",
                "Just what .NET developers need!"
            ];
            let currentLine = 0, charIdx = 0, isDeleting = false, pause = false;
            const el = document.getElementById('typewriter-tagline');

            function typeIt() {
                let display = lines[currentLine].substring(0, charIdx);
                el.textContent = display;

                if (!isDeleting && charIdx < lines[currentLine].length) {
                    charIdx++;
                    setTimeout(typeIt, 65);
                }
                else if (isDeleting && charIdx > 0) {
                    charIdx--;
                    setTimeout(typeIt, 35);
                }
                else if (!isDeleting && charIdx === lines[currentLine].length && !pause) {
                    pause = true;
                    setTimeout(() => { isDeleting = true; pause = false; typeIt(); }, 900);
                }
                else if (isDeleting && charIdx === 0 && !pause) {
                    pause = true;
                    currentLine = (currentLine + 1) % lines.length;
                    setTimeout(() => { isDeleting = false; pause = false; typeIt(); }, 250);
                }
            }
            typeIt();
        });
    </script>

    <script>
        (function() {
            // Optional: Save mode between visits
            if(localStorage.getItem("pref-theme")==="light") document.body.classList.add('light-mode');
            else if(localStorage.getItem("pref-theme")==="dark") document.body.classList.remove('light-mode');

            document.getElementById("darkmode-switch").addEventListener("click", function() {
                document.body.classList.toggle('light-mode');
                // Persist preference
                if(document.body.classList.contains("light-mode")){
                    localStorage.setItem("pref-theme", "light");
                } else {
                    localStorage.setItem("pref-theme", "dark");
                }
            });
        })();
    </script>

    <script>
        function copyDemoCode() {
          const code = document.getElementById("demo-snippet").innerText;
          navigator.clipboard.writeText(code);
          const btn = document.querySelector('.copy-btn');
          btn.textContent = 'Copied!';
          btn.classList.add('copied');
          setTimeout(() => {
            btn.textContent = 'Copy';
            btn.classList.remove('copied');
          }, 1300);
        }
    </script>

    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>
